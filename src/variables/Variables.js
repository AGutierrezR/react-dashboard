// Para Axios
const axiosURL = {
  clients: 'http://localhost:3001/clients'
}

// Para las tablas
const thClientArray = ["ID", "Nombre", "Apellidos", "Email", "Acciones"];

module.exports = {
  axiosURL,
  thClientArray
};