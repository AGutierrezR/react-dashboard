# Proyecto de Dashboard

Para instalar todas las dependencias del proyecto

```shell
npm install
```

Con todas las dependencias y paquetes instalados podemos iniciar el proyecto con

```shell
npm run show
```